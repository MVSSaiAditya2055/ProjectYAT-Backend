spring.datasource.url=jdbc:mysql://root:MlAWfOiOTKzulcAxOSteakyBmFcHINnQ@switchback.proxy.rlwy.net:28346/railway
spring.datasource.username=root
spring.datasource.password=MlAWfOiOTKzulcAxOSteakyBmFcHINnQ
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Frontend origins allowed to call the API. Override in Railway if you use a custom domain.
app.cors.allowed-origin-patterns=http://localhost:*,http://127.0.0.1:*,https://*.up.railway.app,https://*.vercel.app

# Enable Demo Mode to display OTP on the frontend (useful if SMTP is not working)
app.demo-mode=true

# ── JWT Configuration ────────────────────────────────────────────────────────────
# IMPORTANT: Change jwt.secret-key to a strong random value in production!
# Generate with: `openssl rand -base64 32`
jwt.secret-key=your-super-secret-key-change-this-in-production-with-openssl-rand-base64-32
# Access token expires in 15 minutes (in milliseconds)
jwt.access-token-expiry=900000
# Refresh token expires in 7 days (in milliseconds)
jwt.refresh-token-expiry=604800000

## SMTP configuration — use environment variables for credentials
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
# Do NOT store real passwords in source. Set SMTP_USER and SMTP_PASS in your environment.
spring.mail.username=${SMTP_USER:saiadityamvs27.ai@gmail.com}
spring.mail.password=${SMTP_PASS:rlov yxfx yjth criv}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=${SMTP_HOST:smtp.gmail.com}
spring.mail.properties.mail.debug=${SMTP_DEBUG:false}
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# ── File Storage ──────────────────────────────────────────────────────────────
# Paths are relative to the backend project root
file.upload.assignments-dir=ProjectYAT-Backend/uploads/assignments
file.upload.submissions-dir=ProjectYAT-Backend/uploads/submissions
file.upload.videos-dir=ProjectYAT-Backend/uploads/videos

# ── Multipart / File upload limits ────────────────────────────────────────────
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

server.port=${PORT:8080}